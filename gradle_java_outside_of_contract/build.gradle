apply plugin: 'java'

sourceSets {
  main {
    java {
      srcDir "src"
    }
    resources {
      srcDir "src"
    }
  }
  // Add new source set
  integrationTest
}

processResources {
  includeEmptyDirs = false
}
